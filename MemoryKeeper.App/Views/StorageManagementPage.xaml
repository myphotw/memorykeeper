<Page
    x:Class="MemoryKeeper.App.Views.StorageManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:MemoryKeeper.App.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <ScrollViewer>
        <StackPanel MaxWidth="720" Spacing="16" Padding="4">
            <TextBlock Text="MemoryKeeper 저장소" FontSize="28" FontWeight="SemiBold" />

            <StackPanel Spacing="10">
                <TextBlock Text="현재 위치" FontWeight="SemiBold" />
                <TextBlock
                    Text="{Binding PhotoRootPath, Mode=OneWay}"
                    TextWrapping="Wrap"
                    IsTextSelectionEnabled="True"
                    Opacity="0.85" />
                <Button Content="폴더 변경" Command="{Binding ChangeFolderCommand}" />
            </StackPanel>

            <StackPanel Spacing="6">
                <TextBlock Text="상태" FontWeight="SemiBold" />
                <TextBlock
                    Text="❌ 접근할 수 없습니다."
                    Visibility="{Binding ShowConnectionError, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                    Foreground="#C42B1C" />
                <StackPanel
                    Spacing="4"
                    Visibility="{Binding ShowConnectionDetails, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding ReadableStatusText, Mode=OneWay}" />
                    <TextBlock Text="{Binding WritableStatusText, Mode=OneWay}" />
                </StackPanel>
                <Button Content="연결 확인" Command="{Binding CheckConnectionCommand}" />
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage, Mode=OneWay}" TextWrapping="Wrap" Opacity="0.85" />
            <ProgressRing IsActive="{Binding IsBusy, Mode=OneWay}" Width="32" Height="32" HorizontalAlignment="Left" />
        </StackPanel>
    </ScrollViewer>
</Page>

<Page
    x:Class="MemoryKeeper.App.Views.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:MemoryKeeper.App.Models"
    xmlns:converters="using:MemoryKeeper.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid RowDefinitions="Auto,Auto,*" RowSpacing="12">
        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="뒤로" Command="{Binding GoBackCommand}" Padding="10,4" />
            <TextBlock Text="즐겨찾기" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center" />
        </StackPanel>
            <TextBlock
            Grid.Row="1"
            Text="{Binding StatusMessage, Mode=OneWay}"
            Opacity="0.8"
            TextWrapping="Wrap" />

        <GridView
            Grid.Row="2"
            ItemsSource="{Binding Items, Mode=OneWay}"
            IsItemClickEnabled="True"
            ItemClick="Favorites_OnItemClick"
            SelectionMode="None">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Border
                        Width="168"
                        Height="196"
                        Margin="4"
                        Padding="8"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8">
                        <Grid RowDefinitions="*,Auto" RowSpacing="6">
                            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="4">
                                <Grid>
                                    <ProgressRing
                                        Width="28"
                                        Height="28"
                                        IsActive="{Binding IsThumbnailLoading, Mode=OneWay}"
                                        Visibility="{Binding IsThumbnailLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    <Image Stretch="UniformToFill" Source="{Binding ThumbnailImage, Mode=OneWay}" />
                                    <TextBlock
                                        Text="★"
                                        FontSize="16"
                                        Foreground="#E6A700"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="0,4,6,0" />
                                </Grid>
                            </Border>
                            <StackPanel Grid.Row="1" Spacing="2">
                                <TextBlock
                                    Text="{Binding FileName}"
                                    TextTrimming="CharacterEllipsis"
                                    MaxLines="1"
                                    FontSize="12"
                                    FontWeight="SemiBold" />
                                <TextBlock
                                    Text="{Binding CapturedAtText}"
                                    FontSize="11"
                                    Opacity="0.7" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>

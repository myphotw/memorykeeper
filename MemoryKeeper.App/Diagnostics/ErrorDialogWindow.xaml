<Window
    x:Class="MemoryKeeper.App.Diagnostics.ErrorDialogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="12">
        <TextBlock
            x:Name="TitleText"
            FontSize="20"
            FontWeight="SemiBold"
            TextWrapping="Wrap" />

        <TextBox
            x:Name="ErrorTextBox"
            Grid.Row="1"
            IsReadOnly="True"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ScrollViewer.HorizontalScrollBarVisibility="Auto"
            FontFamily="Consolas"
            FontSize="13"
            MinHeight="280" />

        <TextBlock
            x:Name="StatusText"
            Grid.Row="2"
            Opacity="0.75"
            TextWrapping="Wrap" />

        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            Spacing="8"
            HorizontalAlignment="Right">
            <Button Content="전체 복사" Click="CopyAll_OnClick" />
            <Button Content="로그 열기" Click="OpenLog_OnClick" />
            <Button Content="닫기" Click="Close_OnClick" />
        </StackPanel>
    </Grid>
</Window>

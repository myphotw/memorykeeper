<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Empty state — never leave a blank page -->
    <Style x:Key="MkEmptyStateCardStyle" TargetType="Border" BasedOn="{StaticResource MkCardStyle}">
        <Setter Property="Padding" Value="40,36" />
        <Setter Property="MaxWidth" Value="480" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="24" />
    </Style>

    <Style x:Key="MkEmptyStateTitleStyle" TargetType="TextBlock" BasedOn="{StaticResource MkSectionTitleStyle}">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>

    <Style x:Key="MkEmptyStateMessageStyle" TargetType="TextBlock" BasedOn="{StaticResource MkCaptionTextStyle}">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,0,0,20" />
    </Style>
</ResourceDictionary>

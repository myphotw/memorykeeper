<Window
    x:Class="MemoryKeeper.App.Diagnostics.ErrorDialogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" RowDefinitions="Auto,*,Auto,Auto" RowSpacing="12">
        <TextBlock x:ConnectionId='2'
            x:Name="TitleText"
            FontSize="20"
            FontWeight="SemiBold"
            TextWrapping="Wrap" />

        <TextBox x:ConnectionId='3'
            x:Name="ErrorTextBox"
            Grid.Row="1"
            IsReadOnly="True"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ScrollViewer.HorizontalScrollBarVisibility="Auto"
            FontFamily="Consolas"
            FontSize="13"
            MinHeight="280" />

        <TextBlock x:ConnectionId='4'
            x:Name="StatusText"
            Grid.Row="2"
            Opacity="0.75"
            TextWrapping="Wrap" />

        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            Spacing="8"
            HorizontalAlignment="Right">
            <Button x:ConnectionId='5' Content="전체 복사"                         />
            <Button x:ConnectionId='6' Content="로그 열기"                         />
            <Button x:ConnectionId='7' Content="닫기"                       />
        </StackPanel>
    </Grid>
</Window>


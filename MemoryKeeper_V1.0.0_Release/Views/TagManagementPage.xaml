<Page
    x:Class="MemoryKeeper.App.Views.TagManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:MemoryKeeper.App.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid RowDefinitions="Auto,*,Auto" ColumnDefinitions="2*,3*" ColumnSpacing="24" RowSpacing="16">
        <StackPanel Grid.ColumnSpan="2" Spacing="4">
            <TextBlock Text="태그 관리" FontSize="24" FontWeight="SemiBold" />
        </StackPanel>

        <Grid Grid.Row="1" Grid.Column="0" RowDefinitions="Auto,*" RowSpacing="8">
            <TextBlock Text="태그 목록" FontWeight="SemiBold" />
            <ListView
                Grid.Row="1"
                ItemsSource="{Binding Tags, Mode=OneWay}"
                SelectedItem="{Binding SelectedTag, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Spacing="2" Padding="0,6">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                <TextBlock
                                    Text="고정"
                                    Foreground="#0A7A3E"
                                    Visibility="{Binding IsPinned, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </StackPanel>
                            <TextBlock Opacity="0.75" FontSize="12">
                                <Run Text="사용 사진 " />
                                <Run Text="{Binding UsageCount}" />
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <ScrollViewer Grid.Row="1" Grid.Column="1">
            <StackPanel Spacing="10">
                <TextBlock Text="태그 편집" FontWeight="SemiBold" />
                <TextBox
                    Header="이름"
                    Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <CheckBox
                    Content="목록 상단 고정"
                    IsChecked="{Binding IsPinned, Mode=TwoWay}" />
                <TextBlock
                    Text="이름 변경과 삭제가 가능합니다. 병합은 추후 지원합니다."
                    Opacity="0.75"
                    TextWrapping="Wrap" />

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="새로 작성" Command="{Binding ClearFormCommand}" />
                    <Button Content="추가" Style="{StaticResource AccentButtonStyle}" Command="{Binding CreateCommand}" />
                    <Button Content="이름 변경" Command="{Binding RenameCommand}" />
                    <Button Content="고정 저장" Command="{Binding SavePinnedCommand}" />
                    <Button x:ConnectionId='2' Content="삭제"                        />
                    <Button Content="새로고침" Command="{Binding LoadCommand}" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <TextBlock
            Grid.Row="2"
            Grid.ColumnSpan="2"
            Text="{Binding StatusMessage, Mode=OneWay}"
            TextWrapping="Wrap"
            Opacity="0.85" />
    </Grid>
</Page>

